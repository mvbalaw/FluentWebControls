# Sample artifact path note for the PowerShell build.
# The build no longer reads YAML settings. Pass a custom artifact folder directly:
#
#    .\Build.bat -Artifacts "\\server\share\FluentWebControls"

